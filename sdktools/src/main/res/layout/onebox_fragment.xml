<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="@color/white"
    android:clickable="true"
    android:orientation="vertical">


    <RelativeLayout
        android:id="@+id/search_container"
        android:layout_width="match_parent"
        android:layout_height="56dp"
        android:background="@color/navigate_blue_button"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/search_back_button"
            android:layout_width="56dp"
            android:layout_height="match_parent"
            android:background="@drawable/ripple_white_bounded_circle"
            android:src="@drawable/icon_back_white" />

        <EditText
            android:id="@+id/search_field_editable"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginLeft="56dp"
            android:background="@android:color/transparent"
            android:gravity="center_vertical"
            android:hint="Search text here"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:maxLines="1"
            android:paddingLeft="2dp"
            android:singleLine="true"
            android:textColor="#ffffff"
           />

        <ImageButton
            android:id="@+id/search_clear"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_alignParentRight="true"
            android:background="@drawable/ripple_white_bounded_circle"
            android:elevation="3dp"
            android:visibility="gone"
            android:src="@drawable/icon_cancel_003_white" />
    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/helper"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:clipChildren="false">

        <include layout="@layout/onebox_helper_layout" />


    </RelativeLayout>

</LinearLayout>